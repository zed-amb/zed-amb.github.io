"use strict"
const prompt = require("prompt-sync")();

/**
 * 
 * @param {*} a anything 
 * @param {*} b anything
 */

function swap(a, b) {
    let temp = a;
    a = b;
    b = temp;
}

let x = 5;
let y = 10;
console.log("expect 10, 5", x, y);



function pow(x, n){
    return null;
}

let a = pow(2, 3);
console.log(a)
